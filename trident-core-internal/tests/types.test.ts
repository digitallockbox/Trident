describe("Core Types", () => {
    it("defines EngineConfig type placeholder", () => {
        const config = { name: "A-engine", version: "0.0.1" };
        expect(config.name).toBeDefined();
    });
});
