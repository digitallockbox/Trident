name: Deploy Frontend

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ["frontend/**"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -f infra/docker/frontend.Dockerfile -t trident-frontend:${{ github.sha }} frontend/
      - name: Push to registry
        run: echo "TODO: Push to container registry"
      - name: Deploy
        run: echo "TODO: Deploy to environment"
