// Solana instruction builder skeleton
export function buildPayoutInstruction(params: { destination: string; amountLamports: number }) {
    // TODO: Build Solana instruction for payout
    return { instruction: "mock-instruction", ...params };
}
